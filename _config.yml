# Site
name: "Geek on Film"
title: "Geek on Film"
description: "Home of the reviews and podcast for the hosts of Geek On Film.  Great conversations ahead, spoilers ahoy!"
logo: 'assets/images/gof-logo.png'
wide-logo: 'assets/images/logo-wide.png'
favicon: 'assets/images/favicon.ico'
google_analytics: 'G-K0FZMDHZKE'
disqus: 'geek-on-film'
mailchimp-list: 'https://geekonfilm.us14.list-manage.com/subscribe/post?u=5dba67fa5e66c0a8fe02ce5ef&amp;id=eb11fc6452'
mailchimp-logo: 'assets/images/mailchimp-footer.png'
twitter: https://twitter.com/GeekOnFilmCom
twitter-handle: GeekOnFilmCom
include: ["_pages"]

# Authors
authors:
  robbie:
    name: Robbie Holmes
    display_name: RobbieTheGeek
    avatar: 'assets/images/robbie-avatar.png'
    email: robbiethegeek+gof@gmail.com
    web: https://www.robbiethegeek.com
    twitter: https://twitter.com/RobbieTheGeek
    letterboxd: https://letterboxd.com/robbiethegeek/
    serializd: https://srlzd.com/u/robbiethegeek
    description: "Known as RobbieTheGeek everywhere online, Robbie is a podcaster, technologist, amateur cinephile, home chef & tech community organizer."
  jon:
    name: Jon Hoche 
    display_name: Jon Hoche    
    avatar: 'assets/images/jon-avatar.png'
    email: jonhoche@gmail.com
    web: http://jonhoche.com/
    twitter: https://twitter.com/jonhoche
    letterboxd: https://letterboxd.com/jonhoche/
    description: "Jon can often be seen on a stage or screen, an actor and puppeteer that also embraces his nerdy reading comics, catching up on the latest TV and film."

# Plugins
plugins:
  - jekyll-paginate-v2
  - jekyll-sitemap
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-archives
  - jekyll-redirect-from
    
# Archives
jekyll-archives:
  enabled:
    - categories
  layout: archive
  permalinks:
    category: '/category/:name/'
    
# Pagination 
# paginate: 6
# paginate_path: /page:num/

############################################################
# Site configuration for the Jekyll 3 Pagination Gem
# The values here represent the defaults if nothing is set
pagination:
  
  # Site-wide kill switch, disabled here it doesn't run at all 
  enabled: true

  # Set to 'true' to enable pagination debugging. This can be enabled in the site config or only for individual pagination pages
  debug: false

  # The default document collection to paginate if nothing is specified ('posts' is default)
  collection: 'posts'

  # How many objects per paginated page, used to be `paginate` (default: 0, means all)
  per_page: 6

  # The permalink structure for the paginated pages (this can be any level deep)
  permalink: '/page/:num/' # Pages are index.html inside this folder (default)
  #permalink: '/page/:num.html' # Pages are simple html files 
  #permalink: '/page/:num' # Pages are html files, linked jekyll extensionless permalink style.

  # Optional the title format for the paginated pages (supports :title for original page title, :num for pagination page number, :max for total number of pages)
  title: ':title - page :num'

  # Limit how many pagenated pages to create (default: 0, means all)
  limit: 0
  
  # Optional, defines the field that the posts should be sorted on (omit to default to 'date')
  sort_field: 'date'

  # Optional, sorts the posts in reverse order (omit to default decending or sort_reverse: true)
  sort_reverse: true

  # Optional, the default category to use, omit or just leave this as 'posts' to get a backwards-compatible behavior (all posts)
  category: 'posts'

  # Optional, the default tag to use, omit to disable
  tag: ''

  # Optional, the default locale to use, omit to disable (depends on a field 'locale' to be specified in the posts, 
  # in reality this can be any value, suggested are the Microsoft locale-codes (e.g. en_US, en_GB) or simply the ISO-639 language code )
  locale: '' 

 # Optional,omit or set both before and after to zero to disable. 
 # Controls how the pagination trail for the paginated pages look like. 
  trail: 
    before: 2
    after: 2

  # Optional, the default file extension for generated pages (e.g html, json, xml).
  # Internally this is set to html by default
  extension: html

  # Optional, the default name of the index file for generated pages (e.g. 'index.html')
  # Without file extension
  indexpage: 'index'

# Other
markdown: kramdown

kramdown:
  input: GFM
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    block:
      line_numbers: true

# Adsense (change to "enabled" to activate, also your client id and ad slot. Create a new ad unit from your Adsense account to get the slot.)
adsense: "disabled"
adsense-data-ad-client: "ca-pub-3412143450191416"
adsense-data-ad-slot: "1363087678"

# Lazy Images ("enabled" or "disabled")
lazyimages: "enabled"

collections:
  podcasts:
    output: true

exclude: [changelog.md, LICENSE.txt, README.md, Gemfile, Gemfile.lock]
